import 'package:flutter/material.dart';

class MealsAppMain extends StatelessWidget {
  const MealsAppMain({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      title: 'Meals App',
    );
  }
}